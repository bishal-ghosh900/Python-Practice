# Tuples

# Tuples are immutable list, so we can't modify it after creating it

tuple = (1, 2, 3, 4, 5, 6, 3, 4, 1)

print(tuple.count(4)) # 2 -> 2 occurence of 4
print(tuple) # (1, 2, 3, 4, 5, 6, 3, 4, 1)
